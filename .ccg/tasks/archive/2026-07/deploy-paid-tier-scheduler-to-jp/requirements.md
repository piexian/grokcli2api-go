# Requirements

- Deploy local commit `fa1ac0b` to the existing `jp` Grok adapter deployment.
- Preserve remote credentials, environment configuration, volumes, and unrelated files.
- Follow the server's existing deployment mechanism instead of replacing it.
- Verify container/process health, startup logs, public health endpoint, model listing, official tier display, paid-first scheduling prerequisites, and billing polling behavior.
- Avoid printing or persisting OAuth tokens, local API keys, administrator keys, emails, or account subjects.
- Record the deployed revision, validation results, and rollback point.
- Do not invoke external multi-model review unless explicitly requested by the user.
